# Cat Fact App 🐱

This is a simple React + Vite project that fetches random cat facts using the Cat Facts API.

## Technologies Used
- React
- Vite
- Fetch API

## API Used
[Cat Facts API](https://catfact.ninja/fact)

## How to Run
1. Clone the repo
2. Run `npm install`
3. Run `npm run dev`
